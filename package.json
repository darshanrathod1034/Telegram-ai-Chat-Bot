{
  "name": "ai-telegram-bot",
  "version": "1.0.0",
  "description": "AI Telegram Bot with Resume and Booking PDF generation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "db:migrate": "node -e \"require('./database').runMigrations()\"",
    "db:cleanup": "node database/cleanup.js",
    "db:stats": "node -e \"const s = require('./services/analyticsService'); s.getBasicStats().then(console.log)\"",
    "db:reset": "node -e \"const {query} = require('./database/connection'); query('DROP TABLE IF EXISTS messages, pdf_generations, conversations, users, daily_stats, migrations CASCADE').then(() => { console.log('Tables dropped'); process.exit(0); })\""
  },
  "keywords": [
    "telegram",
    "bot",
    "ai",
    "nvidia",
    "openai",
    "pdf",
    "postgresql",
    "database",
    "conversation-storage"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "form-data": "^4.0.0",
    "openai": "^4.28.0",
    "pdfkit": "^0.15.0",
    "pg": "^8.20.0"
  }
}
